[build]
  base = "frontend/band-recruiting-app"
  command = "npm install && npx ng build --configuration production --verbose"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  base = "frontend/band-recruiting-app"
  command = "npm install && npx ng build --configuration production"
  publish = "dist/browser"


[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200